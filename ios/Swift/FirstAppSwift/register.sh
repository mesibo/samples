# Authentication token is invalid or unverified. Either verify it with the email that was sent or register a new session.
# reregister if above error is received. 
pod trunk register contact@mesibo.com 'mesibo'
